@model List<DentalCare.Abstraccion.Modelo.Reporteria.ProductoLoteFrecuenciaDto>

<h3 class="rep-titulo">Lotes Más Utilizados</h3>

<div class="exp-table-card">
    <table class="exp-table">
        <thead>
            <tr>
                <th>Código</th>
                <th>Nombre</th>
                <th>Lote</th>
                <th>Cantidad Utilizada</th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="4" class="text-center" style="padding:24px; color:#8a9bb0;">
                        No hay datos de uso registrados.
                    </td>
                </tr>
            }
            else
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td class="td-id">@item.CodigoProducto</td>
                        <td class="td-nombre">@item.NombreProducto</td>
                        <td>@item.Lote</td>
                        <td><span class="stock-num stock-ok">@item.CantidadTotal</span></td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>

<style>
    .rep-titulo {
        font-size: 1.3rem;
        font-weight: 700;
        color: #1e2d3d;
        margin-bottom: 16px;
    }
</style>